# Railway deployment workflow fix

- [ ] Remove the hard dependency on the unknown Railway service name from the deployment workflow.
- [ ] Validate project-scoped Railway deployment using the production environment.
- [ ] Push the workflow fix and verify the resulting production run.
