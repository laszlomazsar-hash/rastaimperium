name: Python Package

on:
  push:
    branches:
      - main
      - master
    paths:
      # REQUIRED scope only: kernel-critical backend/governance contract surfaces.
      # Non-kernel and broad compatibility suites run in optional/non-blocking CI.
      - 'backend/**'
      - 'governance/**'
      - 'tests/test_compliance.py'
      - 'tests/test_deterministic_policy.py'
      - 'tests/test_canonical_json.py'
      - 'tests/test_canonical_hashing.py'
      - 'tests/test_governance_transition_matrix.py'
      - 'tests/test_governance_replayability.py'
      - 'tests/test_topology_atomic_commit.py'
      - 'tests/test_topology_transaction.py'
      - 'infra/scripts/**'
      - 'requirements*.txt'
      - '.github/workflows/python-package-conda.yml'
  pull_request:
    branches:
      - main
      - master
    paths:
      # REQUIRED scope only: kernel-critical backend/governance contract surfaces.
      # Non-kernel and broad compatibility suites run in optional/non-blocking CI.
      - 'backend/**'
      - 'governance/**'
      - 'tests/test_compliance.py'
      - 'tests/test_deterministic_policy.py'
      - 'tests/test_canonical_json.py'
      - 'tests/test_canonical_hashing.py'
      - 'tests/test_governance_transition_matrix.py'
      - 'tests/test_governance_replayability.py'
      - 'tests/test_topology_atomic_commit.py'
      - 'tests/test_topology_transaction.py'
      - 'infra/scripts/**'
      - 'requirements*.txt'
      - '.github/workflows/python-package-conda.yml'
  workflow_dispatch:

jobs:
  build-linux:
    # REQUIRED job: import contract + policy guardrails + kernel-critical tests.
    # Optional legacy/non-kernel compatibility coverage remains non-blocking below.
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: backend/src
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r backend/requirements.txt
        pip install -r backend/requirements-dev.txt
    - name: Validate architecture artifact versions
      run: |
        python infra/scripts/validate_architecture_version.py
    - name: Diagnose Python import path
      env:
        PYTHONPATH: backend/src
      run: |
        python - <<'PY'
        import os
        import pprint
        import sys
        import traceback

        print("Validating codex import contract (codex.compliance must resolve from backend/src)")
        print("sys.path:")
        pprint.pp(sys.path)

        try:
            import codex.compliance as compliance
        except Exception:
            traceback.print_exc()
            with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as summary:
                summary.write("- Import contract failed for `codex.compliance`; likely causes: circular import or shim inversion.\n")
            raise

        print(f"codex.compliance module: {compliance.__file__}")
        PY
    - name: Lint with flake8
      run: |
        # Kernel-only lint target: backend policy-critical code.
        flake8 backend/src --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 backend/src --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Enforce certification profile guardrails
      env:
        PYTHONPATH: backend/src
      run: |
        pytest tests/test_compliance.py -k "profile_c or certification"

    - name: Run required kernel/core test subset
      env:
        PYTHONPATH: backend/src
      run: |
        pytest \
          tests/test_deterministic_policy.py \
          tests/test_canonical_json.py \
          tests/test_canonical_hashing.py \
          tests/test_governance_transition_matrix.py \
          tests/test_governance_replayability.py \
          tests/test_topology_atomic_commit.py \
          tests/test_topology_transaction.py

    - name: Run optional legacy/non-kernel compatibility suite
      continue-on-error: true
      env:
        PYTHONPATH: backend/src
      run: |
        # OPTIONAL scope: broad legacy/non-kernel compatibility checks.
        # Non-blocking by design during migration to preserve required gate stability.
        pytest tests \
          --ignore=tests/test_compliance.py \
          --ignore=tests/test_deterministic_policy.py \
          --ignore=tests/test_canonical_json.py \
          --ignore=tests/test_canonical_hashing.py \
          --ignore=tests/test_governance_transition_matrix.py \
          --ignore=tests/test_governance_replayability.py \
          --ignore=tests/test_topology_atomic_commit.py \
          --ignore=tests/test_topology_transaction.py
